-name: install and run nginx
 host: web
 become: yes
 tasks:
  -name: install nginx
   ansible.bulidin.package:
   state: present
  -name: start the nginx
    ansible.buildin.service:
    name: nginx
    state: started
    enable: yes
 